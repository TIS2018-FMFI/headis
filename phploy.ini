[default]
    scheme = sftp
    user = headis
    host = kempelen.dai.fmph.uniba.sk
    path = /var/www/headis/web
    passive = true

    exclude[] = '.gitignore'

    permissions = 775

    branch = master

    ;post-deploy-remote[] = 'composer install'
